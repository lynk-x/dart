# ─────────────────────────────────────────────────────────
#  Lynk-X PWA — Environment Configuration
#  Use these keys with the --dart-define flag during build:
#  flutter build web --release --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_PUBLISHABLE_KEY=...
# ─────────────────────────────────────────────────────────

# Required for Supabase integration
SUPABASE_URL=your-project-id.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-anon-pub-key

# Required for Firebase Web Push (public VAPID key, not a secret)
FIREBASE_VAPID_KEY=your-vapid-key-here

# Optional: Sentry error reporting
SENTRY_DSN=https://...

# Performance/Debug
ENABLE_CRASH_REPORTING=true
